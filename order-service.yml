server:
  port: 9001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Apigatewaydb
    username: root
    password: Thiru@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # Options for ddl-auto: none, update, create, create-drop, validate
      ddl-auto: update 
    show-sql: true
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  
  
